PORT=4000
NODE_ENV=development
MONGO_URI=mongodb://127.0.0.1:27017/eventguard
CORS_ORIGIN=http://localhost:3000
JWT_SECRET=change-me
JWT_EXPIRES_IN=15m
FRONTEND_URL=http://localhost:3000
# Gmail address to send verification emails from, and an App Password for it
# (Google Account -> Security -> 2-Step Verification -> App passwords).
EMAIL_USER=
EMAIL_APP_PASSWORD=
